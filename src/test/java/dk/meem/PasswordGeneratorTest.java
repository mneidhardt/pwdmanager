package dk.meem;

import static org.junit.Assert.*;

import java.util.List;
import java.util.HashMap;
import java.util.Map;

import org.junit.Test;

public class PasswordGeneratorTest {
/*
	@Test
	public void test() {
		PasswordGenerator pwgen = new PasswordGenerator();
		
		int l1 = 25;
		byte[] pwd1 = pwgen.generate(l1);
		assertEquals(pwd1.length, l1);

		PasswordGenerator pw2 = new PasswordGenerator();
		pw2.setCharset();
		pw2.dumpCharset();
		pw2.runTest(10000000, pw2.charsetSize());
	}

	@Test
	public void test2() {
		Map<Integer, Integer> mymap = new HashMap<Integer, Integer>();
		
    	for (int i=0; i<max; i++) {
            int x = rnd.nextInt(range);
            
            if (mymap.containsKey(x)) {
            	mymap.replace(x, mymap.get(x)+1);
            } else {
            	mymap.put(x,  1);
            }
    	}
    	
    	this.listMap(max, range);
    }

    public void dumpCharset(List<Character> charset) {
    	for (char c : charset) {
    		System.out.print(c);
    	}
    	System.out.println("\nSize of charset: " + charset.size());
    }
    
    public void listMap(int max, int range) {
    	double avgPerBucket = max/range;
    	System.out.println(avgPerBucket);
		for (Map.Entry<Integer, Integer> entry : mymap.entrySet()) {
			System.out.println(entry.getKey() + " = " + entry.getValue() + " Variation: " + Math.abs(avgPerBucket-entry.getValue()));
		}

    }
*/
}
